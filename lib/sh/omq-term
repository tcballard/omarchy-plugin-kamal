#!/usr/bin/env bash
set -euo pipefail
base=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)
exec node -e 'const c=require(process.argv[1]);c.terminal(process.argv[2],process.argv.slice(3)).catch(e=>{console.error(e.message);process.exitCode=1})' "$base/js/core.cjs" "$@"
